parameters:
- name: directory
  type: string

steps:  
- task: Npm@1
  displayName: Install ${{ parameters.name }}
  inputs:
    command: install
    workingDir: lambdas/${{ parameters.name }}

- script: npm test
  displayName: Unit Test ${{ parameters.name }}
  workingDirectory: lambdas/${{ parameters.name }}
