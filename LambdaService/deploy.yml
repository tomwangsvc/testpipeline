parameters:
- name: sources
  type: object

steps:
- ${{ each lambdaName in parameters.sources }}:
  - download: current
    artifact: ${{ lambdaName }}.zip
    displayName: Get ${{ lambdaName }} Artifact