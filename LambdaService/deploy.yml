steps:
- script: mkdir -p terraform/build/
  displayName: Make Lambda .zip Directory

- download: current
  artifact: lambdas
  displayName: Get Lambda Artifacts

- script: mv $(Pipeline.Workspace)/lambdas/* terraform/build
  displayName: Stage Lambda Artifacts
