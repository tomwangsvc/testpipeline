parameters:
- name: directory
  type: string

steps:  
- task: Npm@1
  displayName: Install ${{ parameters.directory }}
  inputs:
    command: install
    workingDir: lambdas/${{ parameters.directory }}

- script: npm test
  displayName: Unit Test ${{ parameters.directory }}
  workingDirectory: lambdas/${{ parameters.directory }}
