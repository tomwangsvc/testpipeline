parameters:
- name: sourceBranchName 
- name: buildReason

variables:
- ${{ if and(ne(variables.Build.SourceBranchName, 'master'), ne(parameters.buildReason, 'PullRequest')) }}:
  - group: 'DEV'

- ${{ if and(ne(parameters.sourceBranchName, 'master'), eq(parameters.buildReason, 'PullRequest')) }}:
  - group: 'QA'

- ${{ if eq(parameters.sourceBranchName, 'master') }}:
  - group: 'UAT'